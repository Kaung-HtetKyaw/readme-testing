// Code generated by BobGen psql v0.41.1. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package bobmodel

import (
	"context"
	"fmt"
	"io"

	"github.com/aarondl/opt/omit"
	"github.com/gofrs/uuid/v5"
	"github.com/stephenafamo/bob"
	"github.com/stephenafamo/bob/dialect/psql"
	"github.com/stephenafamo/bob/dialect/psql/dialect"
	"github.com/stephenafamo/bob/dialect/psql/dm"
	"github.com/stephenafamo/bob/dialect/psql/sm"
	"github.com/stephenafamo/bob/dialect/psql/um"
	"github.com/stephenafamo/bob/expr"
	"github.com/stephenafamo/bob/mods"
	"github.com/stephenafamo/bob/orm"
	"github.com/stephenafamo/bob/types/pgtypes"
)

// SandboxClusterInfo is an object representing the database table.
type SandboxClusterInfo struct {
	SandboxID uuid.UUID `db:"sandbox_id,pk" `
	Name      string    `db:"name" `
	Version   string    `db:"version" `
	Platform  string    `db:"platform" `

	R sandboxClusterInfoR `db:"-" `
}

// SandboxClusterInfoSlice is an alias for a slice of pointers to SandboxClusterInfo.
// This should almost always be used instead of []*SandboxClusterInfo.
type SandboxClusterInfoSlice []*SandboxClusterInfo

// SandboxClusterInfos contains methods to work with the sandbox_cluster_info table
var SandboxClusterInfos = psql.NewTablex[*SandboxClusterInfo, SandboxClusterInfoSlice, *SandboxClusterInfoSetter]("", "sandbox_cluster_info", buildSandboxClusterInfoColumns("sandbox_cluster_info"))

// SandboxClusterInfosQuery is a query on the sandbox_cluster_info table
type SandboxClusterInfosQuery = *psql.ViewQuery[*SandboxClusterInfo, SandboxClusterInfoSlice]

// sandboxClusterInfoR is where relationships are stored.
type sandboxClusterInfoR struct {
	Sandbox *Sandbox // sandbox_cluster_info.sandbox_cluster_info_sandbox_id_fkey
}

func buildSandboxClusterInfoColumns(alias string) sandboxClusterInfoColumns {
	return sandboxClusterInfoColumns{
		ColumnsExpr: expr.NewColumnsExpr(
			"sandbox_id", "name", "version", "platform",
		).WithParent("sandbox_cluster_info"),
		tableAlias: alias,
		SandboxID:  psql.Quote(alias, "sandbox_id"),
		Name:       psql.Quote(alias, "name"),
		Version:    psql.Quote(alias, "version"),
		Platform:   psql.Quote(alias, "platform"),
	}
}

type sandboxClusterInfoColumns struct {
	expr.ColumnsExpr
	tableAlias string
	SandboxID  psql.Expression
	Name       psql.Expression
	Version    psql.Expression
	Platform   psql.Expression
}

func (c sandboxClusterInfoColumns) Alias() string {
	return c.tableAlias
}

func (sandboxClusterInfoColumns) AliasedAs(alias string) sandboxClusterInfoColumns {
	return buildSandboxClusterInfoColumns(alias)
}

// SandboxClusterInfoSetter is used for insert/upsert/update operations
// All values are optional, and do not have to be set
// Generated columns are not included
type SandboxClusterInfoSetter struct {
	SandboxID omit.Val[uuid.UUID] `db:"sandbox_id,pk" `
	Name      omit.Val[string]    `db:"name" `
	Version   omit.Val[string]    `db:"version" `
	Platform  omit.Val[string]    `db:"platform" `
}

func (s SandboxClusterInfoSetter) SetColumns() []string {
	vals := make([]string, 0, 4)
	if s.SandboxID.IsValue() {
		vals = append(vals, "sandbox_id")
	}
	if s.Name.IsValue() {
		vals = append(vals, "name")
	}
	if s.Version.IsValue() {
		vals = append(vals, "version")
	}
	if s.Platform.IsValue() {
		vals = append(vals, "platform")
	}
	return vals
}

func (s SandboxClusterInfoSetter) Overwrite(t *SandboxClusterInfo) {
	if s.SandboxID.IsValue() {
		t.SandboxID = s.SandboxID.MustGet()
	}
	if s.Name.IsValue() {
		t.Name = s.Name.MustGet()
	}
	if s.Version.IsValue() {
		t.Version = s.Version.MustGet()
	}
	if s.Platform.IsValue() {
		t.Platform = s.Platform.MustGet()
	}
}

func (s *SandboxClusterInfoSetter) Apply(q *dialect.InsertQuery) {
	q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
		return SandboxClusterInfos.BeforeInsertHooks.RunHooks(ctx, exec, s)
	})

	q.AppendValues(bob.ExpressionFunc(func(ctx context.Context, w io.Writer, d bob.Dialect, start int) ([]any, error) {
		vals := make([]bob.Expression, 4)
		if s.SandboxID.IsValue() {
			vals[0] = psql.Arg(s.SandboxID.MustGet())
		} else {
			vals[0] = psql.Raw("DEFAULT")
		}

		if s.Name.IsValue() {
			vals[1] = psql.Arg(s.Name.MustGet())
		} else {
			vals[1] = psql.Raw("DEFAULT")
		}

		if s.Version.IsValue() {
			vals[2] = psql.Arg(s.Version.MustGet())
		} else {
			vals[2] = psql.Raw("DEFAULT")
		}

		if s.Platform.IsValue() {
			vals[3] = psql.Arg(s.Platform.MustGet())
		} else {
			vals[3] = psql.Raw("DEFAULT")
		}

		return bob.ExpressSlice(ctx, w, d, start, vals, "", ", ", "")
	}))
}

func (s SandboxClusterInfoSetter) UpdateMod() bob.Mod[*dialect.UpdateQuery] {
	return um.Set(s.Expressions()...)
}

func (s SandboxClusterInfoSetter) Expressions(prefix ...string) []bob.Expression {
	exprs := make([]bob.Expression, 0, 4)

	if s.SandboxID.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "sandbox_id")...),
			psql.Arg(s.SandboxID),
		}})
	}

	if s.Name.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "name")...),
			psql.Arg(s.Name),
		}})
	}

	if s.Version.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "version")...),
			psql.Arg(s.Version),
		}})
	}

	if s.Platform.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "platform")...),
			psql.Arg(s.Platform),
		}})
	}

	return exprs
}

// FindSandboxClusterInfo retrieves a single record by primary key
// If cols is empty Find will return all columns.
func FindSandboxClusterInfo(ctx context.Context, exec bob.Executor, SandboxIDPK uuid.UUID, cols ...string) (*SandboxClusterInfo, error) {
	if len(cols) == 0 {
		return SandboxClusterInfos.Query(
			sm.Where(SandboxClusterInfos.Columns.SandboxID.EQ(psql.Arg(SandboxIDPK))),
		).One(ctx, exec)
	}

	return SandboxClusterInfos.Query(
		sm.Where(SandboxClusterInfos.Columns.SandboxID.EQ(psql.Arg(SandboxIDPK))),
		sm.Columns(SandboxClusterInfos.Columns.Only(cols...)),
	).One(ctx, exec)
}

// SandboxClusterInfoExists checks the presence of a single record by primary key
func SandboxClusterInfoExists(ctx context.Context, exec bob.Executor, SandboxIDPK uuid.UUID) (bool, error) {
	return SandboxClusterInfos.Query(
		sm.Where(SandboxClusterInfos.Columns.SandboxID.EQ(psql.Arg(SandboxIDPK))),
	).Exists(ctx, exec)
}

// AfterQueryHook is called after SandboxClusterInfo is retrieved from the database
func (o *SandboxClusterInfo) AfterQueryHook(ctx context.Context, exec bob.Executor, queryType bob.QueryType) error {
	var err error

	switch queryType {
	case bob.QueryTypeSelect:
		ctx, err = SandboxClusterInfos.AfterSelectHooks.RunHooks(ctx, exec, SandboxClusterInfoSlice{o})
	case bob.QueryTypeInsert:
		ctx, err = SandboxClusterInfos.AfterInsertHooks.RunHooks(ctx, exec, SandboxClusterInfoSlice{o})
	case bob.QueryTypeUpdate:
		ctx, err = SandboxClusterInfos.AfterUpdateHooks.RunHooks(ctx, exec, SandboxClusterInfoSlice{o})
	case bob.QueryTypeDelete:
		ctx, err = SandboxClusterInfos.AfterDeleteHooks.RunHooks(ctx, exec, SandboxClusterInfoSlice{o})
	}

	return err
}

// primaryKeyVals returns the primary key values of the SandboxClusterInfo
func (o *SandboxClusterInfo) primaryKeyVals() bob.Expression {
	return psql.Arg(o.SandboxID)
}

func (o *SandboxClusterInfo) pkEQ() dialect.Expression {
	return psql.Quote("sandbox_cluster_info", "sandbox_id").EQ(bob.ExpressionFunc(func(ctx context.Context, w io.Writer, d bob.Dialect, start int) ([]any, error) {
		return o.primaryKeyVals().WriteSQL(ctx, w, d, start)
	}))
}

// Update uses an executor to update the SandboxClusterInfo
func (o *SandboxClusterInfo) Update(ctx context.Context, exec bob.Executor, s *SandboxClusterInfoSetter) error {
	v, err := SandboxClusterInfos.Update(s.UpdateMod(), um.Where(o.pkEQ())).One(ctx, exec)
	if err != nil {
		return err
	}

	o.R = v.R
	*o = *v

	return nil
}

// Delete deletes a single SandboxClusterInfo record with an executor
func (o *SandboxClusterInfo) Delete(ctx context.Context, exec bob.Executor) error {
	_, err := SandboxClusterInfos.Delete(dm.Where(o.pkEQ())).Exec(ctx, exec)
	return err
}

// Reload refreshes the SandboxClusterInfo using the executor
func (o *SandboxClusterInfo) Reload(ctx context.Context, exec bob.Executor) error {
	o2, err := SandboxClusterInfos.Query(
		sm.Where(SandboxClusterInfos.Columns.SandboxID.EQ(psql.Arg(o.SandboxID))),
	).One(ctx, exec)
	if err != nil {
		return err
	}
	o2.R = o.R
	*o = *o2

	return nil
}

// AfterQueryHook is called after SandboxClusterInfoSlice is retrieved from the database
func (o SandboxClusterInfoSlice) AfterQueryHook(ctx context.Context, exec bob.Executor, queryType bob.QueryType) error {
	var err error

	switch queryType {
	case bob.QueryTypeSelect:
		ctx, err = SandboxClusterInfos.AfterSelectHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeInsert:
		ctx, err = SandboxClusterInfos.AfterInsertHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeUpdate:
		ctx, err = SandboxClusterInfos.AfterUpdateHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeDelete:
		ctx, err = SandboxClusterInfos.AfterDeleteHooks.RunHooks(ctx, exec, o)
	}

	return err
}

func (o SandboxClusterInfoSlice) pkIN() dialect.Expression {
	if len(o) == 0 {
		return psql.Raw("NULL")
	}

	return psql.Quote("sandbox_cluster_info", "sandbox_id").In(bob.ExpressionFunc(func(ctx context.Context, w io.Writer, d bob.Dialect, start int) ([]any, error) {
		pkPairs := make([]bob.Expression, len(o))
		for i, row := range o {
			pkPairs[i] = row.primaryKeyVals()
		}
		return bob.ExpressSlice(ctx, w, d, start, pkPairs, "", ", ", "")
	}))
}

// copyMatchingRows finds models in the given slice that have the same primary key
// then it first copies the existing relationships from the old model to the new model
// and then replaces the old model in the slice with the new model
func (o SandboxClusterInfoSlice) copyMatchingRows(from ...*SandboxClusterInfo) {
	for i, old := range o {
		for _, new := range from {
			if new.SandboxID != old.SandboxID {
				continue
			}
			new.R = old.R
			o[i] = new
			break
		}
	}
}

// UpdateMod modifies an update query with "WHERE primary_key IN (o...)"
func (o SandboxClusterInfoSlice) UpdateMod() bob.Mod[*dialect.UpdateQuery] {
	return bob.ModFunc[*dialect.UpdateQuery](func(q *dialect.UpdateQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return SandboxClusterInfos.BeforeUpdateHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *SandboxClusterInfo:
				o.copyMatchingRows(retrieved)
			case []*SandboxClusterInfo:
				o.copyMatchingRows(retrieved...)
			case SandboxClusterInfoSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a SandboxClusterInfo or a slice of SandboxClusterInfo
				// then run the AfterUpdateHooks on the slice
				_, err = SandboxClusterInfos.AfterUpdateHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))

		q.AppendWhere(o.pkIN())
	})
}

// DeleteMod modifies an delete query with "WHERE primary_key IN (o...)"
func (o SandboxClusterInfoSlice) DeleteMod() bob.Mod[*dialect.DeleteQuery] {
	return bob.ModFunc[*dialect.DeleteQuery](func(q *dialect.DeleteQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return SandboxClusterInfos.BeforeDeleteHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *SandboxClusterInfo:
				o.copyMatchingRows(retrieved)
			case []*SandboxClusterInfo:
				o.copyMatchingRows(retrieved...)
			case SandboxClusterInfoSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a SandboxClusterInfo or a slice of SandboxClusterInfo
				// then run the AfterDeleteHooks on the slice
				_, err = SandboxClusterInfos.AfterDeleteHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))

		q.AppendWhere(o.pkIN())
	})
}

func (o SandboxClusterInfoSlice) UpdateAll(ctx context.Context, exec bob.Executor, vals SandboxClusterInfoSetter) error {
	if len(o) == 0 {
		return nil
	}

	_, err := SandboxClusterInfos.Update(vals.UpdateMod(), o.UpdateMod()).All(ctx, exec)
	return err
}

func (o SandboxClusterInfoSlice) DeleteAll(ctx context.Context, exec bob.Executor) error {
	if len(o) == 0 {
		return nil
	}

	_, err := SandboxClusterInfos.Delete(o.DeleteMod()).Exec(ctx, exec)
	return err
}

func (o SandboxClusterInfoSlice) ReloadAll(ctx context.Context, exec bob.Executor) error {
	if len(o) == 0 {
		return nil
	}

	o2, err := SandboxClusterInfos.Query(sm.Where(o.pkIN())).All(ctx, exec)
	if err != nil {
		return err
	}

	o.copyMatchingRows(o2...)

	return nil
}

// Sandbox starts a query for related objects on sandbox
func (o *SandboxClusterInfo) Sandbox(mods ...bob.Mod[*dialect.SelectQuery]) SandboxesQuery {
	return Sandboxes.Query(append(mods,
		sm.Where(Sandboxes.Columns.ID.EQ(psql.Arg(o.SandboxID))),
	)...)
}

func (os SandboxClusterInfoSlice) Sandbox(mods ...bob.Mod[*dialect.SelectQuery]) SandboxesQuery {
	pkSandboxID := make(pgtypes.Array[uuid.UUID], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkSandboxID = append(pkSandboxID, o.SandboxID)
	}
	PKArgExpr := psql.Select(sm.Columns(
		psql.F("unnest", psql.Cast(psql.Arg(pkSandboxID), "uuid[]")),
	))

	return Sandboxes.Query(append(mods,
		sm.Where(psql.Group(Sandboxes.Columns.ID).OP("IN", PKArgExpr)),
	)...)
}

func attachSandboxClusterInfoSandbox0(ctx context.Context, exec bob.Executor, count int, sandboxClusterInfo0 *SandboxClusterInfo, sandbox1 *Sandbox) (*SandboxClusterInfo, error) {
	setter := &SandboxClusterInfoSetter{
		SandboxID: omit.From(sandbox1.ID),
	}

	err := sandboxClusterInfo0.Update(ctx, exec, setter)
	if err != nil {
		return nil, fmt.Errorf("attachSandboxClusterInfoSandbox0: %w", err)
	}

	return sandboxClusterInfo0, nil
}

func (sandboxClusterInfo0 *SandboxClusterInfo) InsertSandbox(ctx context.Context, exec bob.Executor, related *SandboxSetter) error {
	var err error

	sandbox1, err := Sandboxes.Insert(related).One(ctx, exec)
	if err != nil {
		return fmt.Errorf("inserting related objects: %w", err)
	}

	_, err = attachSandboxClusterInfoSandbox0(ctx, exec, 1, sandboxClusterInfo0, sandbox1)
	if err != nil {
		return err
	}

	sandboxClusterInfo0.R.Sandbox = sandbox1

	sandbox1.R.SandboxClusterInfo = sandboxClusterInfo0

	return nil
}

func (sandboxClusterInfo0 *SandboxClusterInfo) AttachSandbox(ctx context.Context, exec bob.Executor, sandbox1 *Sandbox) error {
	var err error

	_, err = attachSandboxClusterInfoSandbox0(ctx, exec, 1, sandboxClusterInfo0, sandbox1)
	if err != nil {
		return err
	}

	sandboxClusterInfo0.R.Sandbox = sandbox1

	sandbox1.R.SandboxClusterInfo = sandboxClusterInfo0

	return nil
}

type sandboxClusterInfoWhere[Q psql.Filterable] struct {
	SandboxID psql.WhereMod[Q, uuid.UUID]
	Name      psql.WhereMod[Q, string]
	Version   psql.WhereMod[Q, string]
	Platform  psql.WhereMod[Q, string]
}

func (sandboxClusterInfoWhere[Q]) AliasedAs(alias string) sandboxClusterInfoWhere[Q] {
	return buildSandboxClusterInfoWhere[Q](buildSandboxClusterInfoColumns(alias))
}

func buildSandboxClusterInfoWhere[Q psql.Filterable](cols sandboxClusterInfoColumns) sandboxClusterInfoWhere[Q] {
	return sandboxClusterInfoWhere[Q]{
		SandboxID: psql.Where[Q, uuid.UUID](cols.SandboxID),
		Name:      psql.Where[Q, string](cols.Name),
		Version:   psql.Where[Q, string](cols.Version),
		Platform:  psql.Where[Q, string](cols.Platform),
	}
}

func (o *SandboxClusterInfo) Preload(name string, retrieved any) error {
	if o == nil {
		return nil
	}

	switch name {
	case "Sandbox":
		rel, ok := retrieved.(*Sandbox)
		if !ok {
			return fmt.Errorf("sandboxClusterInfo cannot load %T as %q", retrieved, name)
		}

		o.R.Sandbox = rel

		if rel != nil {
			rel.R.SandboxClusterInfo = o
		}
		return nil
	default:
		return fmt.Errorf("sandboxClusterInfo has no relationship %q", name)
	}
}

type sandboxClusterInfoPreloader struct {
	Sandbox func(...psql.PreloadOption) psql.Preloader
}

func buildSandboxClusterInfoPreloader() sandboxClusterInfoPreloader {
	return sandboxClusterInfoPreloader{
		Sandbox: func(opts ...psql.PreloadOption) psql.Preloader {
			return psql.Preload[*Sandbox, SandboxSlice](psql.PreloadRel{
				Name: "Sandbox",
				Sides: []psql.PreloadSide{
					{
						From:        SandboxClusterInfos,
						To:          Sandboxes,
						FromColumns: []string{"sandbox_id"},
						ToColumns:   []string{"id"},
					},
				},
			}, Sandboxes.Columns.Names(), opts...)
		},
	}
}

type sandboxClusterInfoThenLoader[Q orm.Loadable] struct {
	Sandbox func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
}

func buildSandboxClusterInfoThenLoader[Q orm.Loadable]() sandboxClusterInfoThenLoader[Q] {
	type SandboxLoadInterface interface {
		LoadSandbox(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}

	return sandboxClusterInfoThenLoader[Q]{
		Sandbox: thenLoadBuilder[Q](
			"Sandbox",
			func(ctx context.Context, exec bob.Executor, retrieved SandboxLoadInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadSandbox(ctx, exec, mods...)
			},
		),
	}
}

// LoadSandbox loads the sandboxClusterInfo's Sandbox into the .R struct
func (o *SandboxClusterInfo) LoadSandbox(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.Sandbox = nil

	related, err := o.Sandbox(mods...).One(ctx, exec)
	if err != nil {
		return err
	}

	related.R.SandboxClusterInfo = o

	o.R.Sandbox = related
	return nil
}

// LoadSandbox loads the sandboxClusterInfo's Sandbox into the .R struct
func (os SandboxClusterInfoSlice) LoadSandbox(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	sandboxes, err := os.Sandbox(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		for _, rel := range sandboxes {

			if !(o.SandboxID == rel.ID) {
				continue
			}

			rel.R.SandboxClusterInfo = o

			o.R.Sandbox = rel
			break
		}
	}

	return nil
}

type sandboxClusterInfoJoins[Q dialect.Joinable] struct {
	typ     string
	Sandbox modAs[Q, sandboxColumns]
}

func (j sandboxClusterInfoJoins[Q]) aliasedAs(alias string) sandboxClusterInfoJoins[Q] {
	return buildSandboxClusterInfoJoins[Q](buildSandboxClusterInfoColumns(alias), j.typ)
}

func buildSandboxClusterInfoJoins[Q dialect.Joinable](cols sandboxClusterInfoColumns, typ string) sandboxClusterInfoJoins[Q] {
	return sandboxClusterInfoJoins[Q]{
		typ: typ,
		Sandbox: modAs[Q, sandboxColumns]{
			c: Sandboxes.Columns,
			f: func(to sandboxColumns) bob.Mod[Q] {
				mods := make(mods.QueryMods[Q], 0, 1)

				{
					mods = append(mods, dialect.Join[Q](typ, Sandboxes.Name().As(to.Alias())).On(
						to.ID.EQ(cols.SandboxID),
					))
				}

				return mods
			},
		},
	}
}
