// Code generated by BobGen psql v0.41.1. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package factory

import (
	"context"
	"testing"

	"github.com/aarondl/opt/omit"
	"github.com/gofrs/uuid/v5"
	"github.com/jaswdr/faker/v2"
	models "github.com/kubegrade/matrix/postgres/bobmodel"
	"github.com/stephenafamo/bob"
)

type ClusterTagMod interface {
	Apply(context.Context, *ClusterTagTemplate)
}

type ClusterTagModFunc func(context.Context, *ClusterTagTemplate)

func (f ClusterTagModFunc) Apply(ctx context.Context, n *ClusterTagTemplate) {
	f(ctx, n)
}

type ClusterTagModSlice []ClusterTagMod

func (mods ClusterTagModSlice) Apply(ctx context.Context, n *ClusterTagTemplate) {
	for _, f := range mods {
		f.Apply(ctx, n)
	}
}

// ClusterTagTemplate is an object representing the database table.
// all columns are optional and should be set by mods
type ClusterTagTemplate struct {
	ClusterID func() uuid.UUID
	Name      func() string

	r clusterTagR
	f *Factory

	alreadyPersisted bool
}

type clusterTagR struct {
	Cluster *clusterTagRClusterR
}

type clusterTagRClusterR struct {
	o *ClusterTemplate
}

// Apply mods to the ClusterTagTemplate
func (o *ClusterTagTemplate) Apply(ctx context.Context, mods ...ClusterTagMod) {
	for _, mod := range mods {
		mod.Apply(ctx, o)
	}
}

// setModelRels creates and sets the relationships on *models.ClusterTag
// according to the relationships in the template. Nothing is inserted into the db
func (t ClusterTagTemplate) setModelRels(o *models.ClusterTag) {
	if t.r.Cluster != nil {
		rel := t.r.Cluster.o.Build()
		rel.R.ClusterTags = append(rel.R.ClusterTags, o)
		o.ClusterID = rel.ID // h2
		o.R.Cluster = rel
	}
}

// BuildSetter returns an *models.ClusterTagSetter
// this does nothing with the relationship templates
func (o ClusterTagTemplate) BuildSetter() *models.ClusterTagSetter {
	m := &models.ClusterTagSetter{}

	if o.ClusterID != nil {
		val := o.ClusterID()
		m.ClusterID = omit.From(val)
	}
	if o.Name != nil {
		val := o.Name()
		m.Name = omit.From(val)
	}

	return m
}

// BuildManySetter returns an []*models.ClusterTagSetter
// this does nothing with the relationship templates
func (o ClusterTagTemplate) BuildManySetter(number int) []*models.ClusterTagSetter {
	m := make([]*models.ClusterTagSetter, number)

	for i := range m {
		m[i] = o.BuildSetter()
	}

	return m
}

// Build returns an *models.ClusterTag
// Related objects are also created and placed in the .R field
// NOTE: Objects are not inserted into the database. Use ClusterTagTemplate.Create
func (o ClusterTagTemplate) Build() *models.ClusterTag {
	m := &models.ClusterTag{}

	if o.ClusterID != nil {
		m.ClusterID = o.ClusterID()
	}
	if o.Name != nil {
		m.Name = o.Name()
	}

	o.setModelRels(m)

	return m
}

// BuildMany returns an models.ClusterTagSlice
// Related objects are also created and placed in the .R field
// NOTE: Objects are not inserted into the database. Use ClusterTagTemplate.CreateMany
func (o ClusterTagTemplate) BuildMany(number int) models.ClusterTagSlice {
	m := make(models.ClusterTagSlice, number)

	for i := range m {
		m[i] = o.Build()
	}

	return m
}

func ensureCreatableClusterTag(m *models.ClusterTagSetter) {
	if !(m.ClusterID.IsValue()) {
		val := random_uuid_UUID(nil)
		m.ClusterID = omit.From(val)
	}
	if !(m.Name.IsValue()) {
		val := random_string(nil)
		m.Name = omit.From(val)
	}
}

// insertOptRels creates and inserts any optional the relationships on *models.ClusterTag
// according to the relationships in the template.
// any required relationship should have already exist on the model
func (o *ClusterTagTemplate) insertOptRels(ctx context.Context, exec bob.Executor, m *models.ClusterTag) error {
	var err error

	return err
}

// Create builds a clusterTag and inserts it into the database
// Relations objects are also inserted and placed in the .R field
func (o *ClusterTagTemplate) Create(ctx context.Context, exec bob.Executor) (*models.ClusterTag, error) {
	var err error
	opt := o.BuildSetter()
	ensureCreatableClusterTag(opt)

	if o.r.Cluster == nil {
		ClusterTagMods.WithNewCluster().Apply(ctx, o)
	}

	var rel0 *models.Cluster

	if o.r.Cluster.o.alreadyPersisted {
		rel0 = o.r.Cluster.o.Build()
	} else {
		rel0, err = o.r.Cluster.o.Create(ctx, exec)
		if err != nil {
			return nil, err
		}
	}

	opt.ClusterID = omit.From(rel0.ID)

	m, err := models.ClusterTags.Insert(opt).One(ctx, exec)
	if err != nil {
		return nil, err
	}

	m.R.Cluster = rel0

	if err := o.insertOptRels(ctx, exec, m); err != nil {
		return nil, err
	}
	return m, err
}

// MustCreate builds a clusterTag and inserts it into the database
// Relations objects are also inserted and placed in the .R field
// panics if an error occurs
func (o *ClusterTagTemplate) MustCreate(ctx context.Context, exec bob.Executor) *models.ClusterTag {
	m, err := o.Create(ctx, exec)
	if err != nil {
		panic(err)
	}
	return m
}

// CreateOrFail builds a clusterTag and inserts it into the database
// Relations objects are also inserted and placed in the .R field
// It calls `tb.Fatal(err)` on the test/benchmark if an error occurs
func (o *ClusterTagTemplate) CreateOrFail(ctx context.Context, tb testing.TB, exec bob.Executor) *models.ClusterTag {
	tb.Helper()
	m, err := o.Create(ctx, exec)
	if err != nil {
		tb.Fatal(err)
		return nil
	}
	return m
}

// CreateMany builds multiple clusterTags and inserts them into the database
// Relations objects are also inserted and placed in the .R field
func (o ClusterTagTemplate) CreateMany(ctx context.Context, exec bob.Executor, number int) (models.ClusterTagSlice, error) {
	var err error
	m := make(models.ClusterTagSlice, number)

	for i := range m {
		m[i], err = o.Create(ctx, exec)
		if err != nil {
			return nil, err
		}
	}

	return m, nil
}

// MustCreateMany builds multiple clusterTags and inserts them into the database
// Relations objects are also inserted and placed in the .R field
// panics if an error occurs
func (o ClusterTagTemplate) MustCreateMany(ctx context.Context, exec bob.Executor, number int) models.ClusterTagSlice {
	m, err := o.CreateMany(ctx, exec, number)
	if err != nil {
		panic(err)
	}
	return m
}

// CreateManyOrFail builds multiple clusterTags and inserts them into the database
// Relations objects are also inserted and placed in the .R field
// It calls `tb.Fatal(err)` on the test/benchmark if an error occurs
func (o ClusterTagTemplate) CreateManyOrFail(ctx context.Context, tb testing.TB, exec bob.Executor, number int) models.ClusterTagSlice {
	tb.Helper()
	m, err := o.CreateMany(ctx, exec, number)
	if err != nil {
		tb.Fatal(err)
		return nil
	}
	return m
}

// ClusterTag has methods that act as mods for the ClusterTagTemplate
var ClusterTagMods clusterTagMods

type clusterTagMods struct{}

func (m clusterTagMods) RandomizeAllColumns(f *faker.Faker) ClusterTagMod {
	return ClusterTagModSlice{
		ClusterTagMods.RandomClusterID(f),
		ClusterTagMods.RandomName(f),
	}
}

// Set the model columns to this value
func (m clusterTagMods) ClusterID(val uuid.UUID) ClusterTagMod {
	return ClusterTagModFunc(func(_ context.Context, o *ClusterTagTemplate) {
		o.ClusterID = func() uuid.UUID { return val }
	})
}

// Set the Column from the function
func (m clusterTagMods) ClusterIDFunc(f func() uuid.UUID) ClusterTagMod {
	return ClusterTagModFunc(func(_ context.Context, o *ClusterTagTemplate) {
		o.ClusterID = f
	})
}

// Clear any values for the column
func (m clusterTagMods) UnsetClusterID() ClusterTagMod {
	return ClusterTagModFunc(func(_ context.Context, o *ClusterTagTemplate) {
		o.ClusterID = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m clusterTagMods) RandomClusterID(f *faker.Faker) ClusterTagMod {
	return ClusterTagModFunc(func(_ context.Context, o *ClusterTagTemplate) {
		o.ClusterID = func() uuid.UUID {
			return random_uuid_UUID(f)
		}
	})
}

// Set the model columns to this value
func (m clusterTagMods) Name(val string) ClusterTagMod {
	return ClusterTagModFunc(func(_ context.Context, o *ClusterTagTemplate) {
		o.Name = func() string { return val }
	})
}

// Set the Column from the function
func (m clusterTagMods) NameFunc(f func() string) ClusterTagMod {
	return ClusterTagModFunc(func(_ context.Context, o *ClusterTagTemplate) {
		o.Name = f
	})
}

// Clear any values for the column
func (m clusterTagMods) UnsetName() ClusterTagMod {
	return ClusterTagModFunc(func(_ context.Context, o *ClusterTagTemplate) {
		o.Name = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m clusterTagMods) RandomName(f *faker.Faker) ClusterTagMod {
	return ClusterTagModFunc(func(_ context.Context, o *ClusterTagTemplate) {
		o.Name = func() string {
			return random_string(f)
		}
	})
}

func (m clusterTagMods) WithParentsCascading() ClusterTagMod {
	return ClusterTagModFunc(func(ctx context.Context, o *ClusterTagTemplate) {
		if isDone, _ := clusterTagWithParentsCascadingCtx.Value(ctx); isDone {
			return
		}
		ctx = clusterTagWithParentsCascadingCtx.WithValue(ctx, true)
		{

			related := o.f.NewClusterWithContext(ctx, ClusterMods.WithParentsCascading())
			m.WithCluster(related).Apply(ctx, o)
		}
	})
}

func (m clusterTagMods) WithCluster(rel *ClusterTemplate) ClusterTagMod {
	return ClusterTagModFunc(func(ctx context.Context, o *ClusterTagTemplate) {
		o.r.Cluster = &clusterTagRClusterR{
			o: rel,
		}
	})
}

func (m clusterTagMods) WithNewCluster(mods ...ClusterMod) ClusterTagMod {
	return ClusterTagModFunc(func(ctx context.Context, o *ClusterTagTemplate) {
		related := o.f.NewClusterWithContext(ctx, mods...)

		m.WithCluster(related).Apply(ctx, o)
	})
}

func (m clusterTagMods) WithExistingCluster(em *models.Cluster) ClusterTagMod {
	return ClusterTagModFunc(func(ctx context.Context, o *ClusterTagTemplate) {
		o.r.Cluster = &clusterTagRClusterR{
			o: o.f.FromExistingCluster(em),
		}
	})
}

func (m clusterTagMods) WithoutCluster() ClusterTagMod {
	return ClusterTagModFunc(func(ctx context.Context, o *ClusterTagTemplate) {
		o.r.Cluster = nil
	})
}
