// Code generated by BobGen psql v0.41.1. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package factory

import (
	"context"
	"testing"
	"time"

	"github.com/aarondl/opt/omit"
	"github.com/jaswdr/faker/v2"
	models "github.com/kubegrade/matrix/postgres/bobmodel"
	"github.com/stephenafamo/bob"
)

type OAuthMod interface {
	Apply(context.Context, *OAuthTemplate)
}

type OAuthModFunc func(context.Context, *OAuthTemplate)

func (f OAuthModFunc) Apply(ctx context.Context, n *OAuthTemplate) {
	f(ctx, n)
}

type OAuthModSlice []OAuthMod

func (mods OAuthModSlice) Apply(ctx context.Context, n *OAuthTemplate) {
	for _, f := range mods {
		f.Apply(ctx, n)
	}
}

// OAuthTemplate is an object representing the database table.
// all columns are optional and should be set by mods
type OAuthTemplate struct {
	Email     func() string
	Provider  func() string
	CreatedAt func() time.Time
	UpdatedAt func() time.Time

	f *Factory

	alreadyPersisted bool
}

// Apply mods to the OAuthTemplate
func (o *OAuthTemplate) Apply(ctx context.Context, mods ...OAuthMod) {
	for _, mod := range mods {
		mod.Apply(ctx, o)
	}
}

// setModelRels creates and sets the relationships on *models.OAuth
// according to the relationships in the template. Nothing is inserted into the db
func (t OAuthTemplate) setModelRels(o *models.OAuth) {}

// BuildSetter returns an *models.OAuthSetter
// this does nothing with the relationship templates
func (o OAuthTemplate) BuildSetter() *models.OAuthSetter {
	m := &models.OAuthSetter{}

	if o.Email != nil {
		val := o.Email()
		m.Email = omit.From(val)
	}
	if o.Provider != nil {
		val := o.Provider()
		m.Provider = omit.From(val)
	}
	if o.CreatedAt != nil {
		val := o.CreatedAt()
		m.CreatedAt = omit.From(val)
	}
	if o.UpdatedAt != nil {
		val := o.UpdatedAt()
		m.UpdatedAt = omit.From(val)
	}

	return m
}

// BuildManySetter returns an []*models.OAuthSetter
// this does nothing with the relationship templates
func (o OAuthTemplate) BuildManySetter(number int) []*models.OAuthSetter {
	m := make([]*models.OAuthSetter, number)

	for i := range m {
		m[i] = o.BuildSetter()
	}

	return m
}

// Build returns an *models.OAuth
// Related objects are also created and placed in the .R field
// NOTE: Objects are not inserted into the database. Use OAuthTemplate.Create
func (o OAuthTemplate) Build() *models.OAuth {
	m := &models.OAuth{}

	if o.Email != nil {
		m.Email = o.Email()
	}
	if o.Provider != nil {
		m.Provider = o.Provider()
	}
	if o.CreatedAt != nil {
		m.CreatedAt = o.CreatedAt()
	}
	if o.UpdatedAt != nil {
		m.UpdatedAt = o.UpdatedAt()
	}

	o.setModelRels(m)

	return m
}

// BuildMany returns an models.OAuthSlice
// Related objects are also created and placed in the .R field
// NOTE: Objects are not inserted into the database. Use OAuthTemplate.CreateMany
func (o OAuthTemplate) BuildMany(number int) models.OAuthSlice {
	m := make(models.OAuthSlice, number)

	for i := range m {
		m[i] = o.Build()
	}

	return m
}

func ensureCreatableOAuth(m *models.OAuthSetter) {
	if !(m.Email.IsValue()) {
		val := random_string(nil)
		m.Email = omit.From(val)
	}
	if !(m.Provider.IsValue()) {
		val := random_string(nil)
		m.Provider = omit.From(val)
	}
}

// insertOptRels creates and inserts any optional the relationships on *models.OAuth
// according to the relationships in the template.
// any required relationship should have already exist on the model
func (o *OAuthTemplate) insertOptRels(ctx context.Context, exec bob.Executor, m *models.OAuth) error {
	var err error

	return err
}

// Create builds a oauth and inserts it into the database
// Relations objects are also inserted and placed in the .R field
func (o *OAuthTemplate) Create(ctx context.Context, exec bob.Executor) (*models.OAuth, error) {
	var err error
	opt := o.BuildSetter()
	ensureCreatableOAuth(opt)

	m, err := models.OAuths.Insert(opt).One(ctx, exec)
	if err != nil {
		return nil, err
	}

	if err := o.insertOptRels(ctx, exec, m); err != nil {
		return nil, err
	}
	return m, err
}

// MustCreate builds a oauth and inserts it into the database
// Relations objects are also inserted and placed in the .R field
// panics if an error occurs
func (o *OAuthTemplate) MustCreate(ctx context.Context, exec bob.Executor) *models.OAuth {
	m, err := o.Create(ctx, exec)
	if err != nil {
		panic(err)
	}
	return m
}

// CreateOrFail builds a oauth and inserts it into the database
// Relations objects are also inserted and placed in the .R field
// It calls `tb.Fatal(err)` on the test/benchmark if an error occurs
func (o *OAuthTemplate) CreateOrFail(ctx context.Context, tb testing.TB, exec bob.Executor) *models.OAuth {
	tb.Helper()
	m, err := o.Create(ctx, exec)
	if err != nil {
		tb.Fatal(err)
		return nil
	}
	return m
}

// CreateMany builds multiple oauths and inserts them into the database
// Relations objects are also inserted and placed in the .R field
func (o OAuthTemplate) CreateMany(ctx context.Context, exec bob.Executor, number int) (models.OAuthSlice, error) {
	var err error
	m := make(models.OAuthSlice, number)

	for i := range m {
		m[i], err = o.Create(ctx, exec)
		if err != nil {
			return nil, err
		}
	}

	return m, nil
}

// MustCreateMany builds multiple oauths and inserts them into the database
// Relations objects are also inserted and placed in the .R field
// panics if an error occurs
func (o OAuthTemplate) MustCreateMany(ctx context.Context, exec bob.Executor, number int) models.OAuthSlice {
	m, err := o.CreateMany(ctx, exec, number)
	if err != nil {
		panic(err)
	}
	return m
}

// CreateManyOrFail builds multiple oauths and inserts them into the database
// Relations objects are also inserted and placed in the .R field
// It calls `tb.Fatal(err)` on the test/benchmark if an error occurs
func (o OAuthTemplate) CreateManyOrFail(ctx context.Context, tb testing.TB, exec bob.Executor, number int) models.OAuthSlice {
	tb.Helper()
	m, err := o.CreateMany(ctx, exec, number)
	if err != nil {
		tb.Fatal(err)
		return nil
	}
	return m
}

// OAuth has methods that act as mods for the OAuthTemplate
var OAuthMods oauthMods

type oauthMods struct{}

func (m oauthMods) RandomizeAllColumns(f *faker.Faker) OAuthMod {
	return OAuthModSlice{
		OAuthMods.RandomEmail(f),
		OAuthMods.RandomProvider(f),
		OAuthMods.RandomCreatedAt(f),
		OAuthMods.RandomUpdatedAt(f),
	}
}

// Set the model columns to this value
func (m oauthMods) Email(val string) OAuthMod {
	return OAuthModFunc(func(_ context.Context, o *OAuthTemplate) {
		o.Email = func() string { return val }
	})
}

// Set the Column from the function
func (m oauthMods) EmailFunc(f func() string) OAuthMod {
	return OAuthModFunc(func(_ context.Context, o *OAuthTemplate) {
		o.Email = f
	})
}

// Clear any values for the column
func (m oauthMods) UnsetEmail() OAuthMod {
	return OAuthModFunc(func(_ context.Context, o *OAuthTemplate) {
		o.Email = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m oauthMods) RandomEmail(f *faker.Faker) OAuthMod {
	return OAuthModFunc(func(_ context.Context, o *OAuthTemplate) {
		o.Email = func() string {
			return random_string(f)
		}
	})
}

// Set the model columns to this value
func (m oauthMods) Provider(val string) OAuthMod {
	return OAuthModFunc(func(_ context.Context, o *OAuthTemplate) {
		o.Provider = func() string { return val }
	})
}

// Set the Column from the function
func (m oauthMods) ProviderFunc(f func() string) OAuthMod {
	return OAuthModFunc(func(_ context.Context, o *OAuthTemplate) {
		o.Provider = f
	})
}

// Clear any values for the column
func (m oauthMods) UnsetProvider() OAuthMod {
	return OAuthModFunc(func(_ context.Context, o *OAuthTemplate) {
		o.Provider = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m oauthMods) RandomProvider(f *faker.Faker) OAuthMod {
	return OAuthModFunc(func(_ context.Context, o *OAuthTemplate) {
		o.Provider = func() string {
			return random_string(f)
		}
	})
}

// Set the model columns to this value
func (m oauthMods) CreatedAt(val time.Time) OAuthMod {
	return OAuthModFunc(func(_ context.Context, o *OAuthTemplate) {
		o.CreatedAt = func() time.Time { return val }
	})
}

// Set the Column from the function
func (m oauthMods) CreatedAtFunc(f func() time.Time) OAuthMod {
	return OAuthModFunc(func(_ context.Context, o *OAuthTemplate) {
		o.CreatedAt = f
	})
}

// Clear any values for the column
func (m oauthMods) UnsetCreatedAt() OAuthMod {
	return OAuthModFunc(func(_ context.Context, o *OAuthTemplate) {
		o.CreatedAt = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m oauthMods) RandomCreatedAt(f *faker.Faker) OAuthMod {
	return OAuthModFunc(func(_ context.Context, o *OAuthTemplate) {
		o.CreatedAt = func() time.Time {
			return random_time_Time(f)
		}
	})
}

// Set the model columns to this value
func (m oauthMods) UpdatedAt(val time.Time) OAuthMod {
	return OAuthModFunc(func(_ context.Context, o *OAuthTemplate) {
		o.UpdatedAt = func() time.Time { return val }
	})
}

// Set the Column from the function
func (m oauthMods) UpdatedAtFunc(f func() time.Time) OAuthMod {
	return OAuthModFunc(func(_ context.Context, o *OAuthTemplate) {
		o.UpdatedAt = f
	})
}

// Clear any values for the column
func (m oauthMods) UnsetUpdatedAt() OAuthMod {
	return OAuthModFunc(func(_ context.Context, o *OAuthTemplate) {
		o.UpdatedAt = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m oauthMods) RandomUpdatedAt(f *faker.Faker) OAuthMod {
	return OAuthModFunc(func(_ context.Context, o *OAuthTemplate) {
		o.UpdatedAt = func() time.Time {
			return random_time_Time(f)
		}
	})
}

func (m oauthMods) WithParentsCascading() OAuthMod {
	return OAuthModFunc(func(ctx context.Context, o *OAuthTemplate) {
		if isDone, _ := oauthWithParentsCascadingCtx.Value(ctx); isDone {
			return
		}
		ctx = oauthWithParentsCascadingCtx.WithValue(ctx, true)
	})
}
